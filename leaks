while :
do
    echo -n "Enter the program name to run (or 'q' to quit): "
    read program_name
    
    if [ "$program_name" = "q" ]; then
        break
    fi

    # 실행할 프로그램 명칭으로 설정하고 실행
    $program_name

	while :
	do
		leaks $program_name
    	sleep 0.5
	done
done